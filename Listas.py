# Dada una listade números, utiliza un método para crear una nueva listaque contenga el doble de cada número en la listaoriginal:
lista= [2, 7, 8, 10, 14, 3, 4, 20]
print (lista)
for i in range(len(lista)):
    lista[i] *= 2
print(lista)